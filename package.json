{
  "private": true,
  "name": "linkedin-auto-connect",
  "version": "2.15.0",
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run --reporter=verbose",
    "badges": "node scripts/badges.mjs --write",
    "badges:check": "node scripts/badges.mjs --check"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "jsdom": "^25.0.0"
  }
}
